# MoonCurveFit 项目申报书
- 项目名称：MoonCurveFit：面向 MoonBit 领域包的小型稠密残差优化与参数辨识内核
- 参赛者：梁煜婷
- 电话：17871952067
- GitHub：https://github.com/yyyt0807/moonbit-curvefit
- Mooncakes：https://mooncakes.io/docs/yyyt0807/curvefit
- 项目方向：新生态项目建设；通用数值优化基础设施 / 领域模型参数辨识
- 项目性质：原创 MoonBit 实现，AI 辅助，非 SciPy/Ceres 移植；Apache-2.0
## 项目定位与生态差异
MoonBit 已有 `owl_mbt` 的 ndarray/通用一阶优化、`moon-certified` 的线性拟合，也出现电路、吸附等正向仿真领域包；但“根据多条观测反推有物理边界的小量参数”仍需要各领域重复实现差分、约束、鲁棒损失、预算和可识别性判断。MoonCurveFit 0.2 把曲线拟合下沉为通用残差优化内核：领域包只提交带名称、精度权重和可选梯度的标量误差项，就能在 Native/JS/wasm-gc 复用同一套 LM/QR 和逐项诊断，不强制引入 ndarray 或 Python FFI。它与现有包互补，不声称取代通用优化或正向仿真。
## 已交付的三个跨领域完整场景
- IoT/机器人/游戏定位：三个无线电、超声或场景锚点各产生一条距离残差，联合求二维坐标；示例从冗余方程恢复 `(4,3)`，显示解析梯度、边界及每个锚点的目标贡献，证明 API 不依赖 x/y 曲线表。
- 正向仿真的参数辨识：把五个频率点的 RC 低通响应包装为命名残差，恢复增益 `2` 与时间常数 `0.5`。该方式可供电路、材料、吸附等“已有正向模型但缺少反演”的领域包接入；这里仅证明接口模式，不宣称与相关项目已有合作。
- 软件服务容量规划：以六个负载—延迟观测反推基础延迟、容量上限和排队尺度，容量参数设置不低于最大观测负载的边界；示例恢复 `20 ms / 250 rps / 15 ms`，并自动使用完整数值 Jacobian，服务于性能回归、压测和边缘侧离线分析。
- 复现：`moon run examples/ecosystem --target native`，同一场景在 `js` / `wasm-gc` 运行；原有校准、动力学、鲁棒拟合继续作为上层曲线接口示例。
## 核心实现与量化证据
- `ResidualTerm/solve_terms` 提供稳定的生态接缝：残差有名称、精度和可选全参数梯度；仅当全部梯度存在才启用解析 Jacobian，否则统一数值差分，结果返回逐项原始/加权误差和目标贡献，便于上层定位异常观测。
- 提供线性、多项式、指数、Gaussian、4/5 参数 Logistic、Saturation/Hill 及自定义模型；诊断包含停止原因、预算、秩、QR 条件代理、局部协方差、近似区间/均值带及有条件的影响量，不将条件代理称为精确条件数。
- 90 项测试在 wasm、wasm-gc、JS、Native 各全部通过；三个生态场景在 Native/JS/wasm-gc 通过。0.2 精确提交的 Ubuntu/Windows/macOS CI及 Ubuntu 13 个 SciPy 黑盒对照均成功；mooncakes 0.2.0 经全新项目下载并在三后端调用新增 API，证据仅适用于测试集合，不构成任意数据的数值认证。
- 100/1,000/10,000 点的 12 组准确性工作负载通过；指数解析/数值 Jacobian 为 7–8 / 55–56 次残差调用，不虚构时间优势。有效 MoonBit 4,065 行包含测试和示例，库实现为 2,706 行；开发历史超过 20 条有效提交。
## 交付与功能边界
交付残差优化 API、曲线拟合上层、原生 CLI、CSV/JSON/报告、跨领域合成场景、跨平台 CI、独立对照和边界文档。最多 64 参数、100 万 Jacobian 条目；定位不含地图约束，参数辨识不替代正向仿真，容量模型不替代压测；不支持稀疏、全局优化、ODE 或临床推断。劣初值与不可识别模型可失败，调用方必须检查停止原因、秩及领域验收阈值。
