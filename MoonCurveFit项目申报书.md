# MoonCurveFit 项目申报书（供本人改写确认的草稿）
- 项目名称：MoonCurveFit：MoonBit 原生带约束非线性曲线拟合与诊断库
- 参赛者：yyyt0807（正式提交前由本人填写真实姓名及联系方式）
- GitHub：https://github.com/yyyt0807/moonbit-curvefit
- Mooncakes：https://mooncakes.io/docs/yyyt0807/curvefit（0.1.0 已发布并验证独立安装）
- 项目方向：新生态项目建设；通用科学计算基础库 / 测量数据分析工具
- 项目性质：原创 MoonBit 实现，AI 辅助，非 SciPy/Ceres 移植；Apache-2.0
## 项目定位与生态差异
工程和科学测量经常需要在物理边界内估计非线性模型参数，但只返回一组系数会掩盖未收敛、数据不足或参数不可识别。MoonCurveFit 提供从残差求解到诊断和报告的可复用链路，数值核心不依赖 Python 运行时或数值 FFI，可用于 Native 工具及 JS/wasm-gc 应用。owl_mbt 已有通用优化，moon-certified 已有线性/多项式拟合及线性诊断；本项目并非基础数学完全无重合，其独立贡献是带约束、固定值及缩放的非线性残差求解、鲁棒目标和有适用条件的不确定度输出。
## 已交付的三个完整应用场景
- 仪器响应校准：输入带精度权重或重复测量的标定点，选择线性/多项式或自定义响应模型，拟合系数并检查残差、秩和标准误，导出校准报告与 SVG。合成示例的真实斜率为 2.2，得到约 2.19910；权重是精度而非标准差，不自动删除重复点。
- 降解与动力学分析：输入时间—响应数据，为速率设置非负边界，拟合指数幅度、速率和背景并保留停止状态。示例真值为幅度 8、速率 0.7、背景 0.4，拟合速率误差小于 1e-5；适用于材料、化学及工程过程，不输出临床解释或用药建议。
- 离线离群测量分析：对存在异常点的通用响应数据比较普通损失和 Huber 损失，保留目标值、警告及预测，输出 JSON/Markdown/CSV/SVG供上层应用使用。示例真实截距为 2，普通拟合约 3.19355，Huber 约 2.00859；鲁棒结果不提供不适用的普通协方差，且未声称交付完整浏览器 GUI。
- 复现：`moon run examples/demo --target native`，同一示例也可在 `js` / `wasm-gc` 运行；文件工具入口为 `moon run cmd/curvefit --target native -- fit-json examples/data/calibration.json markdown`。
## 核心实现与量化证据
- 缩放及可行投影的局部 Levenberg–Marquardt，列主元 Householder QR，解析/边界差分 Jacobian；支持固定/有界参数、精度权重及 Linear/Huber/SoftL1/Cauchy 目标，显式多起点并记录每次结果。
- 提供线性、多项式、指数、Gaussian、4/5 参数 Logistic、Saturation/Hill 及自定义模型；诊断包含停止原因、预算、秩、QR 条件代理、局部协方差、近似区间/均值带及有条件的影响量，不将条件代理称为精确条件数。
- 86 项测试在 wasm、wasm-gc、JS、Native 各全部通过；Ubuntu/Windows/macOS 远端 CI 成功，Ubuntu 另验证 13 个独立 SciPy 合成黑盒对照，最大归一化参数误差约 8.46e-7、目标误差约 6.23e-14。证据仅适用于测试集合，见 `docs/local-review.md` 和 `docs/release-verification.md`，不构成任意数据的数值认证。
- 100/1,000/10,000 点的 12 组准确性工作负载通过；指数解析/数值 Jacobian 为 7–8 / 55–56 次残差调用，不虚构时间优势。有效 MoonBit 4,065 行包含测试和示例，库实现为 2,706 行；开发历史超过 20 条有效提交。
## 交付与功能边界
交付数值库、原生 CLI、严格数字 CSV/版本化 JSON、报告导出、合成数据场景、跨平台 CI、独立对照工具及数值/安全/许可证说明。最多 64 参数、100 万 Jacobian 条目；不支持稀疏、全局优化、ODE、临床推断或鲁棒 sandwich 协方差。劣初值与不可识别模型可失败，必须检查停止原因及诊断；CLI 全文件读取与回调资源由调用方额外限制。后续优先维护近奇异问题回归、分配性能及独立测量案例，而非扩展为泛化科学平台。
## 人工确认
九月规则要求申报书人工撰写；本文是依据实际工程证据生成的辅助草稿，不能声明已满足该要求。参赛者应本人改写并核对身份、单人一个项目原则、功能及最终发布证据，再在一页 Markdown 内提交；不复制其他项目的个人信息。
